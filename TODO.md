# Field Studies — Nice-to-have TODO

Future improvements, not required for MDW 2026 opening.

1. **Language switch italiano / inglese** — runtime toggle between IT and EN captions (titles, taglines, product labels).
2. **Rotation in every direction (L/R)** — allow either screen to render rotated 90° clockwise or counter-clockwise, for installations where the TV physical orientation differs from the default.
3. **Landscape layout** — 1920×1080 variant of both screens (complete rework of composition, not just a rotation).
4. **Randomized project sequence option** — shuffle the 37 projects on each cycle instead of iterating in fixed array order.
5. **Reduced effects for low-performance browsers** — detect low FPS / underpowered device and drop expensive animations (staggers, image drifts, SVG draws) to a lighter variant.
6. **4K option for hi-res screens** — 2160×3840 portrait asset pipeline and adaptive CSS so the same codebase scales to 4K panels.
7. **Same-browser two-windows option** — ability to open SX and DX side-by-side in a single browser (split viewport or paired windows) for development and for venues where one PC drives two outputs.
8. **Line weights revision** — audit all SVG strokes, accordion borders, and text weights for consistency across the 37 project plans.
9. **DX gallery image width option** — switch between full-width (1080px) gallery images on DX, or images sized to match the SX column width (so both screens share a visual grid).
